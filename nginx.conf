http {
	proxy_cache_path /cache keys_zone=cache:64m;
	server {
		listen 80;

		server_name localhost "";

		location /api/ {
			proxy_cache cache; # TODO: Change for suggestions
			proxy_cache_min_uses 5;
			proxy_cache_valid any 5m;
			proxy_pass http://api:8000/;
		}
	}
}

events {
	
}